# SPHEREx AWS 数据管线：简明使用说明

这份文档用于解释：新程序和原来几个编号脚本有什么关系，以及以后实际使用时应该关注哪些文件。

## 一句话理解这个项目

程序先从 AWS 获取“文件清单”，从清单中选出需要的文件，然后安全地下载 FITS，并记录哪些文件已经成功下载。

```text
AWS 文件清单
    ↓
本地 catalog
    ↓
选择需要的文件
    ↓
下载清单 manifest
    ↓
安全下载和检查 FITS
```

目前不做光谱提取、测光或其他科学分析。

## 和原来程序的关系

原来的学习过程是：

```text
01_list_spherex.py
        ↓
02_make_catalog.py
        ↓
03_parse_catalog.py
        ↓
04_download_test.py
        ↓
05_read_fits.py
```

这些脚本每个只做一件事，适合学习流程，但它们之间主要依靠手工操作：

- 路径写死在脚本里；
- 下载哪个文件需要修改代码；
- 只能通过“文件是否存在”判断是否下载过；
- 没有统一记录失败、更新和重试；
- 从不同目录启动脚本时可能找不到文件。

新程序没有改变原来的科学数据流程，只是把这些步骤组合成一个可以重复运行的工具：

| 原来的脚本 | 新程序中的对应部分 |
|---|---|
| `01_list_spherex.py` | `s3_access.py` |
| `02_make_catalog.py` | `catalog.py` 和 `database.py` |
| `03_parse_catalog.py` | `filenames.py` |
| 手工从 CSV 选择文件 | `selection.py` |
| `04_download_test.py` | `downloader.py` |
| `05_read_fits.py` | `verification.py` |

你不需要逐个运行这些新模块。它们统一由下面这个入口调用：

```bash
python -m spherex_pipeline
```

## 你日常真正需要关注的内容

### 1. `config.toml`

这是最重要的用户配置文件。

它决定：

- 查询哪个 S3 bucket 和 prefix；
- 测试选择多少个文件；
- 从哪个 planning period 选择；
- 下载到哪里；
- 同时下载几个文件；
- 是否验证 FITS 和计算 SHA-256。

当前配置是：

```toml
[selection]
planning_period = "2025W17_4B"
limit = 10
```

因此当前测试始终只选择 10 个 FITS。

### 2. `python -m spherex_pipeline`

这是唯一需要记住的程序入口。后面的单词表示要做哪一步。

### 3. `catalogs/spherex.sqlite3`

这是程序的工作记录本，里面保存：

- AWS 上有哪些对象；
- 对象是否新增或更新；
- 哪些文件已经下载；
- 下载是否成功；
- 文件大小、ETag 和 SHA-256；
- 失败原因和重试次数。

不要用文本编辑器或 Excel 修改这个文件。平时也不需要直接打开它。

### 4. `catalogs/test10_manifest.csv`

这是已经选定的 10 个测试文件清单。

manifest 的意义是把本次选择固定下来。以后反复 debug 时，程序仍然使用同一批文件，而不是每次随机换 10 个。

可以用 Excel 或文本编辑器查看，但暂时不要手工修改。需要重新选择时，应运行 `select --overwrite`。

### 5. `data/qr2/...`

这里保存真正下载的 FITS 数据。

目录结构和 S3 key 保持一致，例如：

```text
data/
└── qr2/
    └── level2/
        └── 2025W17_4B/
            └── l2b-v20-2025-240/
                └── 2/
                    └── level2_....fits
```

以后迁移到 HPC 时，主要需要迁移或重新配置的就是这个数据目录。

## 最简单的日常操作

首先进入环境和项目目录：

```bash
conda activate spherex
cd /mnt/c/Users/26823/Desktop/SPHEREx_project
```

### 查看当前下载状态

```bash
python -m spherex_pipeline status
```

当前应显示：

```text
completed  10 files  683.16 MiB
```

### 只预览，不下载

```bash
python -m spherex_pipeline download --dry-run
```

它会告诉你：

- 哪些文件需要下载；
- 哪些文件已经存在，会跳过；
- 总共要传输多少数据。

在任何较大下载前，都建议先运行这一步。

### 执行下载

```bash
python -m spherex_pipeline download
```

当前 10 个文件都已经完成，所以再次运行时应该全部跳过，不会重复下载。

### 再次检查 10 个 FITS

```bash
python -m spherex_pipeline verify
```

### 更新测试范围的 AWS catalog

```bash
python -m spherex_pipeline catalog update \
  --prefix qr2/level2/2025W17_4B/
```

这一步只获取文件元数据，不下载 FITS。

### 重新生成 10 文件清单

只有明确希望换一批测试文件时才运行：

```bash
python -m spherex_pipeline select --overwrite
```

平常 debug 不需要反复运行它。

## 哪些是项目核心部分

### 使用层面的核心

这些文件最值得关注：

```text
config.toml
spherex_pipeline/
catalogs/spherex.sqlite3
catalogs/test10_manifest.csv
data/qr2/
```

其中你日常通常只需要修改 `config.toml`，然后运行统一命令。

### 程序内部的核心

如果以后要读代码，建议按以下顺序：

1. `cli.py`：看到每个命令会调用什么功能；
2. `config.py`：看到配置如何进入程序；
3. `filenames.py`：理解文件名如何解析；
4. `catalog.py` 和 `database.py`：理解 catalog 更新；
5. `selection.py`：理解如何选出 10 个文件；
6. `downloader.py`：理解安全下载；
7. `verification.py`：理解 FITS 和 SHA-256 检查。

## 已经移出正式项目的练习内容

以下内容已统一移动到：

```text
C:\Users\26823\Desktop\practice
```

- `scripts/01_...` 到 `scripts/05_...`；
- 历史 `level2_catalog.csv`；
- 历史 `level2_catalog_parsed.csv`；
- 空的旧 `data/test/`。

它们没有被删除，可以用来回顾 boto3、CSV 和 FITS 的学习过程，但不再参与正式工作流。归档中的 README 记录了完整构建历史和新旧程序的区别。

## 正式项目中可以暂时不关注的内容

### `catalogs/level2_catalog_current.csv`

这是从 SQLite 导出的便于查看的当前 catalog。

- 查看数据时有用；
- 程序运行并不依赖它；
- 删除后可以通过 `catalog export` 重新生成。

### `tests/`

这是程序自动测试，正常下载时不需要打开。修改代码后才需要运行：

```bash
python -m unittest discover -s tests -v
```

### `logs/`

只有下载失败或需要检查运行历史时才看。

### `notes/pipeline_design.md`

这是较详细的工程设计说明。当前只想使用程序时可以先不读。

## 目前不要删除的文件

即使有些文件平时不用，也建议现在不要删除以下内容：

- `config.toml`
- 整个 `spherex_pipeline/`
- `catalogs/spherex.sqlite3`
- `catalogs/test10_manifest.csv`
- `data/qr2/` 中的 FITS
- `requirements.txt`

练习脚本和历史 CSV 已经在桌面 `practice` 中独立归档，不要再把它们复制回正式项目参与运行。

## 当前阶段的推荐做法

接下来先不要扩大数据量。继续使用现有 10 个文件，重点测试：

1. `status` 是否始终正确；
2. 重复运行 `download` 是否全部跳过；
3. 临时移动一个测试文件后，dry-run 是否能发现缺失；
4. 网络中断后是否能够重新下载；
5. catalog 再次扫描时是否能稳定得到零变化。

等这些操作熟悉并稳定后，再进行完整 `qr2/level2/` 元数据扫描，以及设计真正的科学目标筛选规则。

## 以后如何扩大文件数量

扩大规模时，建议逐级进行，不要直接从 10 个跳到全部数据：

```text
10 个 -> 100 个 -> 1000 个 -> 一个 planning period -> 多个 period -> 全部数据
```

### 情况一：仍使用 `2025W17_4B`，从 10 个增加到 100 个

先建立一个新的 manifest，保留原来的 10 文件测试清单：

```bash
python -m spherex_pipeline select \
  --limit 100 \
  --manifest catalogs/test100_manifest.csv
```

先检查预计下载量：

```bash
python -m spherex_pipeline download \
  --manifest catalogs/test100_manifest.csv \
  --dry-run
```

确认磁盘空间和传输量后再下载：

```bash
python -m spherex_pipeline download \
  --manifest catalogs/test100_manifest.csv
```

不要使用 `--overwrite` 覆盖 `test10_manifest.csv`，这样原来的 10 文件回归测试可以一直保留。

按照当前文件约 68.32 MiB 的大小粗略估算：

- 100 个约 6.7 GiB；
- 1000 个约 66.7 GiB。

实际运行前仍应以 `--dry-run` 给出的总量为准。

### 情况二：测试另一个 planning period

假设要测试 `2025W18_1A`，先只扫描这个 period 的元数据：

```bash
python -m spherex_pipeline catalog update \
  --prefix qr2/level2/2025W18_1A/
```

然后建立独立 manifest：

```bash
python -m spherex_pipeline select \
  --planning-period 2025W18_1A \
  --limit 100 \
  --manifest catalogs/2025W18_1A_100_manifest.csv
```

再执行 dry-run 和下载。

上面的 planning period 只是命令格式示例。实际使用前应先从 AWS catalog 确认该 period 确实存在，不应猜测目录名。

### 情况三：扫描全部 QR2 Level 2 元数据

运行：

```bash
python -m spherex_pipeline catalog update
```

`config.toml` 中的默认 prefix 是：

```text
qr2/level2/
```

因此这个命令会列出该 prefix 下的全部对象并更新 SQLite，但不会下载 FITS。完成后先导出 catalog：

```bash
python -m spherex_pipeline catalog export
```

然后检查总文件数和总容量，再决定下载范围。

### 情况四：将来需要下载全部数据

当前版本故意要求 `select` 提供一个整数 `limit`，而且一次只方便筛选一个 planning period。它还没有开放“一条命令选择全部数据”的功能。这是为了防止测试阶段意外开始数百 GiB 或更大的下载。

真正准备下载全部数据时，不建议把 `limit` 随意写成一个极大的数字。应先给程序增加一个明确的 `select --all` 模式，并至少执行以下保护：

1. 完成整个 `qr2/level2/` catalog 扫描；
2. 统计对象数和总字节数；
3. 检查本地或 HPC 可用空间；
4. 生成单独的全量 manifest；
5. 将 manifest 分批，例如每批 500 或 1000 个文件；
6. dry-run 后要求明确确认；
7. 下载过程中允许按批次恢复；
8. 在 HPC 上限制同时写入 catalog 的进程数量。

因此，当前扩大到 100 或 1000 个文件不需要修改程序；真正的“全量下载”阶段建议先扩展程序，而不是直接修改配置硬跑。
